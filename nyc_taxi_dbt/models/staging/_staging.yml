version: 2

sources:
  - name: raw
    database: NYC_TAXI_DB
    schema: RAW
    tables:
      - name: trips
        description: Raw NYC Yellow Taxi trip records
      - name: taxi_zones
        description: NYC Taxi Zone lookup

models:
  - name: stg_trips
    description: Staged trips with corrected data types
  - name: stg_taxi_zones
    description: Staged zone lookup
